diff -r -u a/debian/compat b/debian/compat
--- a/debian/compat
+++ b/debian/compat
@@ -1 +1 @@
-10
+9
diff -r -u a/debian/control b/debian/control
--- a/debian/control
+++ b/debian/control
@@ -2,7 +2,7 @@
 Maintainer: Julien Lamy <lamy@unistra.fr>
 Section: science
 Priority: optional
-Build-Depends: debhelper (>= 10), 
+Build-Depends: debhelper (>= 9), 
                d-shlibs,
                dh-python,
                cmake,
diff -r -u a/debian/rules b/debian/rules
--- a/debian/rules
+++ b/debian/rules
@@ -40,7 +40,7 @@
 	  -DPYTHON_EXECUTABLE=/usr/bin/python$(PYTHON3)
 
 override_dh_makeshlibs-arch:
-	dh_makeshlibs -V'libdicomifier1 (>= 1.2.1-1)'
+	dh_makeshlibs -V'libdicomifier1 (>= 1.2.1-1~jessie)'
 
 # WARNING: Python2 and Python3 MUST be tested sequentially since they use the 
 # same build directory
@@ -76,6 +76,7 @@
 		--override s/libdcm.*-dev/libdcmtk-dev/ \
 		--override s/libof.*-dev/libdcmtk-dev/ \
 		--override s/libjsoncpp.-dev/libjsoncpp-dev/ \
+		--override s/libboost_date_time.*-dev/libboost-date-time-dev/ \
 		debian/tmp/usr/lib/*.so
 
 override_dh_clean:
