diff --git a/debian/changelog b/debian/changelog
index c5118c6..60cbcf6 100644
--- a/debian/changelog
+++ b/debian/changelog
@@ -1,3 +1,9 @@
+odil (0.9.1-1~xenial) xenial; urgency=medium
+
+  * Backport to xenial
+
+ -- Julien Lamy <lamy@unistra.fr>  Fri, 09 Mar 2018 11:28:50 +0000
+
 odil (0.9.1-1) unstable; urgency=medium
 
   [ Julien Lamy ]
diff --git a/debian/control b/debian/control
index 3c86b04..f82803e 100644
--- a/debian/control
+++ b/debian/control
@@ -3,11 +3,11 @@ Maintainer: Debian Med Packaging Team <debian-med-packaging@lists.alioth.debian.
 Uploaders: Julien Lamy <lamy@unistra.fr>
 Section: science
 Priority: optional
-Build-Depends: debhelper (>= 10),
-               d-shlibs (>= 0.81),
+Build-Depends: debhelper (>= 9),
+               d-shlibs,
                dh-python,
                cmake,
-               g++ (>= 4:7),
+               g++ (>= 4:5),
                pkg-config,
                libjsoncpp-dev,
                libdcmtk-dev | libdcmtk2-dev,
diff --git a/debian/rules b/debian/rules
index ae7efce..723de3a 100755
--- a/debian/rules
+++ b/debian/rules
@@ -54,7 +54,7 @@ override_dh_auto_build-indep:
 	find documentation/_build -name "*.md5" -delete
 
 override_dh_makeshlibs-arch:
-	dh_makeshlibs -V'libodil0 (>= 0.9.1-1)'
+	dh_makeshlibs -V'libodil0 (>= 0.9.1-1~xenial)'
 
 # Run C++ tests only on base build
 override_dh_auto_test-arch-nopy:
@@ -109,6 +109,14 @@ override_dh_install-arch:
 		--devunversioned \
 		--commit --multiarch --exclude-a --exclude-la \
 		--movedev debian/tmp/usr/include/* usr/include \
+		--override s/libjsoncpp.-dev/libjsoncpp-dev/ \
+		--override s/libdcm.*-dev/libdcmtk-dev/ \
+		--override s/libof.*-dev/libdcmtk-dev/ \
+		--override s/libicuuc.*-dev/libicu-dev/ \
+		--override s/libboost_date_time.*-dev/libboost-date-time-dev/ \
+		--override s/libboost_filesystem.*-dev/libboost-filesystem-dev/ \
+		--override s/libboost_regex.*-dev/libboost-regex-dev/ \
+		--override s/libboost_system.*-dev/libboost-system-dev/ \
 		debian/tmp/usr/lib/*.so
 
 
